services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/flyway_manager
      PORT: 3000
      NODE_ENV: production
    restart: unless-stopped
